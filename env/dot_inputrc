#Enable searching through history using up/down arrows
"\e[A":history-search-backward
"\e[B":history-search-forward

###Alter the default behavior of the completion functions###

#Set to on causes the matches to be listed immediately instead of ringing the bell
set show-all-if-ambiguous on

#Ignore case in auto-completion
set completion-ignore-case on
